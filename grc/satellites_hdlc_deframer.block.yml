# auto-generated by grc.converter

id: satellites_hdlc_deframer
label: HDLC Deframer
category: '[Satellites]/AX.25'

parameters:
-   id: check_fcs
    label: Check FCS
    dtype: bool
    default: 'True'
    options: ['True', 'False']
-   id: max_length
    label: Maximum frame length (bytes)
    dtype: int
    default: '10000'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.hdlc_deframer(check_fcs=${check_fcs}, max_length=${max_length})

file_format: 1
