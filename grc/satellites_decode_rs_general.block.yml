# auto-generated by grc.converter

id: satellites_decode_rs_general
label: Reed-Solomon Decoder (libfec)
category: '[Satellites]/FEC'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: gfpoly
    label: Finite field generator polynomial
    dtype: int
-   id: fcr
    label: First consecutive root (index)
    dtype: int
-   id: prim
    label: Primitive element (index)
    dtype: int
-   id: nroots
    label: Number of roots
    dtype: int

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.decode_rs_general(${gfpoly}, ${fcr}, ${prim}, ${nroots}, ${verbose})

file_format: 1
