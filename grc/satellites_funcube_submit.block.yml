# auto-generated by grc.converter

id: satellites_funcube_submit
label: FUNcube Telemetry Forwarder
category: '[Satellites]/Misc'

parameters:
-   id: url
    label: URL
    dtype: string
    default: http://data.amsat-uk.org
-   id: site_id
    label: Site Id
    dtype: string
-   id: auth_code
    label: Auth Code
    dtype: string

inputs:
-   domain: message
    id: in

templates:
    imports: import satellites
    make: satellites.funcube_submit(${url}, ${site_id}, ${auth_code})

file_format: 1
