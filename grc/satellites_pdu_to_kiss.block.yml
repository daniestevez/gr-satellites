# auto-generated by grc.converter

id: satellites_pdu_to_kiss
label: PDU to KISS
category: '[Satellites]/KISS'

parameters:
-   id: control_byte
    label: Include control byte
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.pdu_to_kiss(${control_byte})

file_format: 1
