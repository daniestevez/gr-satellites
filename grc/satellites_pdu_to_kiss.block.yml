# auto-generated by grc.converter

id: satellites_pdu_to_kiss
label: PDU to KISS
category: '[Satellites]/KISS'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.pdu_to_kiss()

file_format: 1
