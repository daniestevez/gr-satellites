# auto-generated by grc.converter

id: satellites_print_timestamp
label: Print timestamp
category: '[Satellites]/Misc'

parameters:
-   id: fmt
    label: Format
    dtype: string
    default: '%Y-%m-%d %H:%M:%S'
-   id: count
    label: Packet counter
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.print_timestamp(${fmt}, ${count})

file_format: 1
