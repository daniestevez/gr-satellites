# auto-generated by grc.converter

id: satellites_kiss_to_pdu
label: KISS to PDU
category: '[Satellites]/KISS'

parameters:
-   id: control_byte
    label: Expect control byte
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.kiss_to_pdu(${control_byte})

file_format: 1
