id: satellites_si4463_scrambler
label: SI4463 scrambler
category: '[Satellites]/Scrambling'

inputs:
-   label: in
    domain: message
    dtype: message

outputs:
-   label: out
    domain: message
    dtype: message

templates:
    imports: import satellites.hier
    make: satellites.hier.si4463_scrambler()

file_format: 1
