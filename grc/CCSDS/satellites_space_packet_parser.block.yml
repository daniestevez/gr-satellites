# auto-generated by grc.converter

id: satellites_space_packet_parser
label: Space Packet Parser
category: '[Satellites]/CCSDS'

parameters:
-   id: time_header
    label: Time Header
    dtype: enum
    default: drop_down
    options: ['0', '1']
    option_labels: ['Yes', 'No']
-   id: id_time
    label: Time Format ID
    dtype: raw
    default: '''default_value'''
    hide: ${ ('none' if time_header == '0' else 'all') }
-   id: time_format
    label: Time Format
    dtype: enum
    default: drop_down
    options: ['0', '1', '2', '3', '4']
    option_labels: [CUC, CDS, CCS, ASCII A, ASCII B]
    hide: ${ ('none' if time_header == '0' else 'all') }
-   id: pfield
    label: PField Presence
    dtype: enum
    default: drop_down
    options: ['1', '0']
    option_labels: ['Yes', 'No']
    hide: ${ ('all' if time_format > '2' or time_header == '1' else 'none') }

inputs:
-   domain: message
    id: in

templates:
    imports: import satellites
    make: satellites.space_packet_parser(${time_header}, ${time_format}, ${pfield},
        ${id_time})

file_format: 1
