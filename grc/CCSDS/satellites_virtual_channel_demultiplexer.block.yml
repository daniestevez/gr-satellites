# auto-generated by grc.converter

id: satellites_virtual_channel_demultiplexer
label: Virtual Channel Demultiplexer
category: '[Satellites]/CCSDS'

parameters:
-   id: vc_outputs
    label: Virtual Channel ID outputs
    dtype: int_vector
    hide: none

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
    multiplicity: ${ len(vc_outputs) }
-   domain: message
    id: discarded

templates:
    imports: import satellites
    make: satellites.virtual_channel_demultiplexer((${vc_outputs}))

file_format: 1
