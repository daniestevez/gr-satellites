# auto-generated by grc.converter

id: satellites_append_crc32c
label: Append CRC-32C
category: '[Satellites]/CSP'

parameters:
-   id: include_header
    label: Include CSP header
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.append_crc32c(${include_header})

file_format: 1
