id: satellites_smogp_packet_filter
label: SMOG-P/ATL-1 packet filter
category: '[Satellites]/Telemetry'

parameters:

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.smogp_packet_filter()

file_format: 1
