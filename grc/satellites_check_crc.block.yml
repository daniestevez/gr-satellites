# auto-generated by grc.converter

id: satellites_check_crc
label: Check CRC
category: '[Satellites]/CSP'

parameters:
-   id: include_header
    label: Include header
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: force
    label: Force
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: ok
    optional: true
-   domain: message
    id: fail
    optional: true

templates:
    imports: import satellites
    make: satellites.check_crc(${include_header}, ${verbose}, ${force})

file_format: 1
