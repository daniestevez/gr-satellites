# auto-generated by grc.converter

id: satellites_fixedlen_tagger
label: Fixed Length Packet Tagger (Deprecated)
category: '[Satellites]/Deprecated'

parameters:
-   id: stream_type
    label: Stream Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        t: [numpy.complex64, numpy.float32, numpy.int, numpy.short, numpy.byte]
    hide: part
-   id: syncword_tag
    label: Syncword tag
    dtype: string
    default: syncword
-   id: packetlen_tag
    label: Packet length tag
    dtype: string
    default: packet_len
-   id: packet_len
    label: Packet length
    dtype: int

inputs:
-   domain: stream
    dtype: ${ stream_type }

outputs:
-   domain: stream
    dtype: ${ stream_type }

templates:
    imports: |-
        import satellites
        import numpy
    make: satellites.fixedlen_tagger(${syncword_tag}, ${packetlen_tag}, ${packet_len},
        ${stream_type.t})
    callbacks:
    - set_packet_len(${packet_len})

file_format: 1
