# auto-generated by grc.converter

id: satellites_submit
label: Telemetry Forwarder
category: '[Satellites]/Misc'

parameters:
-   id: url
    label: URL
    dtype: string
-   id: noradID
    label: NORAD ID
    dtype: int
-   id: source
    label: Receiver callsign
    dtype: string
-   id: latitude
    label: Latitude (N is +, S is -)
    dtype: float
-   id: longitude
    label: Longitude (W is -, E is +)
    dtype: float
-   id: tstamp
    label: Start of recording UTC (blank for realtime)
    dtype: string

inputs:
-   domain: message
    id: in

templates:
    imports: import satellites
    make: satellites.submit(${url}, ${noradID}, ${source}, ${longitude}, ${latitude},
        ${tstamp})

file_format: 1
