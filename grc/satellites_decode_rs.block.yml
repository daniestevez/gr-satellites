# auto-generated by grc.converter

id: satellites_decode_rs
label: CCSDS Reed-Solomon Decoder
category: '[Satellites]/FEC'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: basis
    label: Basis
    dtype: int
    options: ['0', '1']
    option_labels: [Conventional, Dual]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.decode_rs(${verbose}, ${basis})

file_format: 1
