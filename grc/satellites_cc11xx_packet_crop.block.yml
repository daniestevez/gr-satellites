# auto-generated by grc.converter

id: satellites_cc11xx_packet_crop
label: CC11xx packet crop
category: '[Satellites]/CC11xx'

parameters:
-   id: use_crc16
    label: Use CRC16
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.cc11xx_packet_crop(${use_crc16})

file_format: 1
