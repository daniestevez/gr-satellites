# auto-generated by grc.converter

id: satellites_ax100_decode
label: NanoCom AX100 decode
category: '[Satellites]/GOMspace'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.ax100_decode(${verbose})

file_format: 1
