# auto-generated by grc.converter

id: satellites_check_swiatowid_crc
label: Check Swiatowid CRC-16
category: '[Satellites]/CRC'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: ok
    optional: true
-   domain: message
    id: fail
    optional: true

templates:
    imports: import satellites
    make: satellites.check_swiatowid_crc(${verbose})

file_format: 1
