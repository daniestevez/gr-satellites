# auto-generated by grc.converter

id: satellites_eseo_packet_crop
label: ESEO Packet Crop
category: '[Satellites]/ESEO'

parameters:
-   id: drop_rs
    label: Drop Reed-Solomon
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.eseo_packet_crop(${drop_rs})

file_format: 1
