# auto-generated by grc.converter

id: satellites_ao40_rs_decoder
label: AO-40 interleaved Reed-Solomon decoders
category: '[Satellites]/FEC'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.ao40_rs_decoder(${verbose})

file_format: 1
