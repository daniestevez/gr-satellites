# auto-generated by grc.converter

id: satellites_telecommand_primaryheader_adder
label: Telecommand Primary Header Adder
category: '[Satellites]/CCSDS'

parameters:
-   id: transfer_frame_version
    label: Transfer Frame Version
    dtype: raw
    default: '0'
    hide: all
-   id: bypass
    label: Bypass
    dtype: raw
    default: '0'
    hide: none
-   id: control
    label: Control
    dtype: raw
    default: '0'
    hide: none
-   id: rsvd_space
    label: RSVD Spare
    dtype: raw
    default: '0'
    hide: all
-   id: spacecraft_id
    label: Spacecraft ID
    dtype: raw
    default: '0'
    hide: none
-   id: virtual_channel_id
    label: Virtual Channel ID
    dtype: raw
    default: '0'
    hide: none
-   id: frame_length
    label: Frame Length
    dtype: raw
    default: '0'
    hide: all
-   id: frame_sequence_number
    label: Frame Sequence Number
    dtype: raw
    default: '0'
    hide: all

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites.ccsds
    make: satellites.ccsds.telecommand_primaryheader_adder(${bypass}, ${control}, ${spacecraft_id},
        ${virtual_channel_id})

file_format: 1
