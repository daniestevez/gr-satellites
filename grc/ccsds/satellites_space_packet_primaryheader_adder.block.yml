# auto-generated by grc.converter

id: satellites_space_packet_primaryheader_adder
label: Space Packet Primary Header Adder
category: '[Satellites]/CCSDS'

parameters:
-   id: ccsds_version
    label: CCSDS Version
    dtype: raw
    default: '0'
    hide: all
-   id: packet_type
    label: Packet Type
    dtype: enum
    default: drop_down
    options: ['0', '1']
    option_labels: [Telemetry, Telecommand]
-   id: secondary_header_flag
    label: Secondary Header Flag
    dtype: enum
    default: drop_down
    options: ['1', '0']
    option_labels: ['Yes', 'No']
    hide: none
-   id: AP_ID
    label: Application ID
    dtype: raw
    default: '0'
    hide: none
-   id: sequence_flags
    label: Sequence Flags
    dtype: raw
    default: '3'
    hide: all
-   id: count_or_name
    label: Count or Name
    dtype: enum
    default: drop_down
    options: ['0', '1']
    option_labels: [Count, Name]
    hide: ${ ('none' if packet_type == '1' else 'all') }
-   id: packet_sequence_count
    label: Packet Sequence Count
    dtype: raw
    default: '0'
    hide: all
-   id: packet_sequence_name
    label: Packet Sequence Name
    dtype: raw
    default: '0'
    hide: ${ ('none' if count_or_name == '1' and packet_type == '1' else 'all') }
-   id: data_length
    label: Data Length
    dtype: raw
    default: '40000'
    hide: all

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites.ccsds
    make: satellites.ccsds.space_packet_primaryheader_adder(${packet_type}, ${secondary_header_flag},
        ${AP_ID}, ${count_or_name}, ${packet_sequence_name})

file_format: 1
