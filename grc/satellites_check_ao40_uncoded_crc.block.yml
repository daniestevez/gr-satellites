# auto-generated by grc.converter

id: satellites_check_ao40_uncoded_crc
label: Check AO-40 CRC (uncoded)
category: '[Satellites]/Packet'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: ok
    optional: true
-   domain: message
    id: fail
    optional: true

templates:
    imports: import satellites
    make: satellites.check_ao40_uncoded_crc(${verbose})

file_format: 1
