# auto-generated by grc.converter

id: satellites_varlen_packet_tagger
label: Variable Length Packet Tagger (Deprecated)
category: '[Satellites]/Deprecated'

parameters:
-   id: syncword_tag
    label: Syncword Tag
    dtype: string
    default: syncword
-   id: packetlen_tag
    label: Packet Length Tag
    dtype: string
    default: packet_len
-   id: length_field_size
    label: Packet Length Size
    dtype: int
    default: '12'
-   id: mtu
    label: MTU
    dtype: int
    default: 255*8
-   id: endianness
    label: Endianness
    dtype: raw
    default: gr.GR_MSB_FIRST
    options: [gr.GR_MSB_FIRST, gr.GR_LSB_FIRST]
    option_labels: [MSB, LSB]
-   id: use_golay
    label: Golay Decoding
    dtype: bool
    default: 'True'
    options: ['False', 'True']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import satellites
    make: satellites.varlen_packet_tagger(${syncword_tag}, ${packetlen_tag}, ${length_field_size},
        ${mtu}, ${endianness}, ${use_golay})

documentation: |-
    Converts a stream of data into a tagged stream.

        The packet length is extracted from a header in the stream.

file_format: 1
