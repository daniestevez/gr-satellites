# auto-generated by grc.converter

id: satellites_ao40_syncframe_soft
label: AO-40 synchronize frame (soft symbols)
category: '[Satellites]/Packet'

parameters:
-   id: threshold
    label: Threshold
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.ao40_syncframe_soft(${threshold})

file_format: 1
