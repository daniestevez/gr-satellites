# auto-generated by grc.converter

id: satellites_encode_rs
label: CCSDS Reed-Solomon Encoder
category: '[Satellites]/FEC'

parameters:
-   id: basis
    label: Basis
    dtype: int
    options: ['0', '1']
    option_labels: [Conventional, Dual]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.encode_rs(${basis})

file_format: 1
