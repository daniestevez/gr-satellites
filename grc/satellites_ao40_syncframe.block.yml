# auto-generated by grc.converter

id: satellites_ao40_syncframe
label: AO-40 synchronize frame
category: '[Satellites]/Packet'

parameters:
-   id: threshold
    label: Threshold
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.ao40_syncframe(${threshold})

file_format: 1
