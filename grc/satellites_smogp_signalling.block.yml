id: satellites_smogp_signalling
label: SMOG-P Signalling Process
category: '[Satellites]/Telemetry'

inputs:
-   domain: message
    id: in

templates:
    imports: import satellites
    make: satellites.smogp_signalling()

file_format: 1
