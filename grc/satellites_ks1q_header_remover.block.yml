# auto-generated by grc.converter

id: satellites_ks1q_header_remover
label: KS-1Q header remover
category: '[Satellites]/Packet'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.ks1q_header_remover(${verbose})

file_format: 1
