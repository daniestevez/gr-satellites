# auto-generated by grc.converter

id: satellites_rscode_decoder
label: Reed-Solomon Decoder (rscode)
category: '[Satellites]/FEC'

parameters:
-   id: npar
    label: Parity check bytes
    dtype: int

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.rscode_decoder(${npar})

file_format: 1
